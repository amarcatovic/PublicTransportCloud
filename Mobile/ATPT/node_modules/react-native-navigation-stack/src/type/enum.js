/**
 * 切换方式
 * current:瞬间到当前，pushCurrent:进入方式到当前<--，backCurrent:退出方式到当前-->，pushHide:-->,backHide<--:
 */
export type tSwitch = 'none' | 'current' | 'pushCurrent' | 'backCurrent' | 'pushHide' | 'backHide';
